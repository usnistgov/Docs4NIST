name: "Build Documentation"

on: [push, pull_request, delete]

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: usnistgov/NISTtheDocs2Death@refactor_sphinxdocs
        with:
          docs-folder: docs/
          formats: |-
            epub
            pdf
